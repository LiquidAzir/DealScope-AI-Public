# Optional — only needed if the frontend connects directly to Neo4j
# The backend handles all graph writes; these vars are used for client-side queries only
VITE_NEO4J_URI=neo4j+s://<your-instance>.databases.neo4j.io
VITE_NEO4J_USER=neo4j
VITE_NEO4J_PASSWORD=...
